using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CannonStatus : MonoBehaviour
{
    [SerializeField]
    private int hp;

    // Start is called before the first frame update
    public void Start()
    {
        //Debug.Log("CannonStatus StartFunction Start");
        //Debug.Log("CannonStatus StartFunction End");
    }

    // Update is called once per frame
    public void Update()
    {
        //Debug.Log("CannonStatus UpdateFunction Start");
        //Debug.Log("CannonStatus UpdateFunction End");
    }
}
